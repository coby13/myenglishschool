	<% if user_signed_in? %>
	<%= simple_form_for(@article, :html => { :multipart => true }) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :prebody %><br />
<%= f.input :prebody, :as => :ckeditor %>

  </div>
  <div class="field">
    <%= f.label :body %><br />
	<%= f.input :body, :as => :ckeditor %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>

 <div class="filed">
	
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% end %>
